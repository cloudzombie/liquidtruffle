{
  "name": "bare-hyperevm",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "node scripts/deploy-testnet.cjs",
    "deploy:mainnet": "npx hardhat run scripts/deploy.cjs --network hyperevm"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  }
}
